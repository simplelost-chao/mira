# Mira 项目规范

## 通用行为准则

基于 Andrej Karpathy 的 LLM 编码常见错误总结。偏向谨慎而非速度，简单任务自行判断。

### 先思考再编码
- 明确说出你的假设。不确定就问。
- 多种理解方式时列出来，不要默默选一个。
- 有更简单的方案就说出来。该反驳时反驳。
- 不清楚就停下来，说明困惑点，提问。

### 简单优先
- 不加没要求的功能。
- 不为一次性代码创建抽象。
- 不加没要求的"灵活性"或"可配置性"。
- 不为不可能的场景做错误处理。
- 200 行能用 50 行写就重写。

### 精准改动
- 不"改进"周围的代码、注释或格式。
- 不重构没坏的东西。匹配现有风格。
- 发现无关死代码，提一句——不要删。
- 你的改动产生的孤儿代码（import/变量/函数）要清理；之前就存在的不要动。
- 每一行改动都应直接溯源到用户的请求。

### 目标驱动执行
- 把任务转化为可验证的目标。
- 多步任务先列简要计划，每步附验证方式。

## 调试规则

- 诊断 bug 时，先充分分析实际错误再建议修复。不要急于下结论或过早归咎于某个组件。提出诊断时必须附上证据。
- 当用户说 bug 的原因是 X，信任他们的诊断并修复 X。除非有具体证据，不要将问题重新解释为其他原因。

## 代码风格

- 保持改动最小化和精准。不要添加用户没有要求的动画、CSS 效果或架构变更。不确定时先问再加。

## UI / 前端

- 做完 UI 或布局变更后，在移动端视口尺寸下验证。本项目经常面向移动端 Safari。注意 viewport 抖动、padding 问题和 overflow。

## API 集成

- 集成第三方 API（Steam、豆瓣、飞书、QQ 音乐等）时，先用小型测试请求验证确切的 API 端点、鉴权方式和响应格式，再构建完整集成。不要假设选择器或 ID。
